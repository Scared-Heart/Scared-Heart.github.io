---
import Base from '../layouts/Base.astro';
import Icon from '../components/Icon.astro';
import { getPosts } from '../lib/posts';
const posts = await getPosts();
---

<Base title="关于"
  ><header class="page-heading">
    <p class="eyebrow">BEHIND THE NOTEBOOK</p><h1>你好，我是 Breeze。</h1><p>
      这里是 apple pie，我在互联网上的一小块地方。
    </p>
  </header><div class="page-content about-layout">
    <div class="about-copy">
      <p>这个博客从 2019 年开始，记录技术探索，也记录平凡的一天。</p><p>
        从树莓派、服务器，到 NAS
        安全事件，这里留下了遇到的问题、排查的过程和解决的办法。比起只有一个结果，也希望把寻找答案的过程留存下来。
      </p><blockquote>不必宏大，值得记下就好。</blockquote><p>
        目前有 {posts.length} 篇文章。你可以从<a href="/archives/">时间归档</a>开始翻阅，也可以通过
        RSS 订阅新的记录。
      </p><div class="about-links">
        <a class="text-link" href="https://github.com/Scared-Heart"
          ><Icon name="github" size={18} /> GitHub ↗</a
        ><a class="text-link" href="/rss.xml"><Icon name="rss" size={18} /> 订阅 RSS</a>
      </div>
    </div><figure>
      <img
        src="/img/banner-preview.webp"
        alt="橙色天空下独自行走的身影"
        width="340"
        height="320"
      /><figcaption>apple pie · 始于 2019</figcaption>
    </figure>
  </div></Base
>
<style>
  .about-layout {
    display: grid;
    grid-template-columns: 1fr 300px;
    gap: 70px;
  }
  .about-copy {
    max-width: 650px;
    font-size: 16px;
    line-height: 2;
  }
  .about-copy > p:first-child {
    margin-top: 0;
  }
  .about-copy a:not(.text-link) {
    color: var(--accent);
    text-decoration: underline;
    text-underline-offset: 4px;
  }
  blockquote {
    margin: 30px 0;
    padding: 10px 0 10px 22px;
    border-left: 3px solid var(--accent);
    font: 24px/1.6 var(--display);
    color: var(--ink);
  }
  figure {
    margin: 0;
  }
  figure img {
    object-fit: cover;
    width: 100%;
    height: 310px;
    border-radius: 5px;
  }
  figcaption {
    font-size: 11px;
    color: var(--muted);
    margin-top: 9px;
  }
  .about-links {
    display: flex;
    gap: 30px;
    margin-top: 30px;
  }
  @media (max-width: 750px) {
    .about-layout {
      grid-template-columns: 1fr;
      gap: 25px;
    }
    .about-layout figure {
      display: none;
    }
    .about-copy {
      font-size: 15px;
    }
  }
</style>
